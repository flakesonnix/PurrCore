# PurrCore

Paper & Spigot — Kotlin — shared core.

DB (HikariCP sqlite/mysql/pg) + I18n (en/de).

```bash
gradle shadowJar
# → build/libs/purrcore-1.0.0.jar
```

Other plugins: `depend: [PurrCore]`.
